---
phase: 01-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [".gitignore", "LICENSE", "CLAUDE.md", "README.md", "CHANGELOG.md", "docs/CONTRIBUTING.md", "docs/ARCHITECTURE.md", "docs/TROUBLESHOOTING.md", "docs/MODDING-NOTES.md", "LaCompta.sln", "LaCompta/LaCompta.csproj", "LaCompta/manifest.json", "LaCompta/ModEntry.cs", "LaCompta/Models/DailyRecord.cs", "LaCompta/Models/ItemTransaction.cs", "LaCompta/Models/SeasonSummary.cs", "LaCompta/Models/FishRecord.cs", "LaCompta/Data/DatabaseContext.cs", "LaCompta/Data/Repository.cs"]
autonomous: false
---

<objective>
## Goal
Set up dev environment, create mod skeleton, and build the complete data layer (models + SQLite + repository).

## Purpose
Combines scaffolding + data layer to minimize plans. Foundation for all subsequent work.

## Output
- .NET 6 SDK + SMAPI installed
- Compilable mod loaded by SMAPI
- SQLite schema + repository layer ready for event hooks (Plan 01-02)
- Documentation foundation: README, CONTRIBUTING, ARCHITECTURE, TROUBLESHOOTING, MODDING-NOTES, CHANGELOG
</objective>

<context>
@.paul/PROJECT.md
@.paul/ROADMAP.md

## Technical References
- Game path: /home/bcalvet/.local/share/Steam/steamapps/common/Stardew Valley/
- Game runtime: .NET 6
- NuGet: Pathoschild.Stardew.ModBuildConfig, Microsoft.Data.Sqlite
</context>

<skills>
No specialized skills required (infrastructure only).
</skills>

<acceptance_criteria>

## AC-1: Dev Environment Ready
```gherkin
Given fresh setup
When running `dotnet --version` and checking SMAPI
Then .NET 6.x SDK works and SMAPI is in game directory
```

## AC-2: Mod Compiles and Loads
```gherkin
Given LaCompta project with all deps
When `dotnet build` runs
Then build succeeds and SMAPI shows "LaCompta 0.1.0 by bcalvet"
```

## AC-3: SQLite Schema + Repository
```gherkin
Given mod loads on SaveLoaded
When database initializes
Then tables exist: daily_records, item_transactions, season_summaries, fish_records
And repository CRUD methods compile
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Dev environment + mod project skeleton</name>
  <files>.gitignore, LICENSE, CLAUDE.md, LaCompta.sln, LaCompta/LaCompta.csproj, LaCompta/manifest.json, LaCompta/ModEntry.cs</files>
  <action>
    Parallel agent work:
    - Agent A: Install .NET 6 SDK + download/install SMAPI
    - Agent B: Create solution, classlib, add NuGets, manifest.json, ModEntry.cs, .gitignore, LICENSE, CLAUDE.md
    Then: dotnet build to verify

    manifest.json: UniqueID "bcalvet.LaCompta", v0.1.0, MinimumApiVersion "4.0.0"
    ModEntry.cs: Extend Mod, log "LaCompta loaded", subscribe DayStarted placeholder

    Documentation (parallel agent):
    - README.md: Project description, features list, installation guide, screenshots placeholder, usage, links
    - CHANGELOG.md: Initialize with v0.1.0 (unreleased)
    - docs/CONTRIBUTING.md: Dev setup (SDK, SMAPI, build, test), project structure, PR guidelines
    - docs/ARCHITECTURE.md: Mod architecture diagram (data flow: game events → tracker → SQLite → dashboard/sheets)
    - docs/TROUBLESHOOTING.md: Start with known setup issues encountered
    - docs/MODDING-NOTES.md: SMAPI tricks, game data quirks, undocumented behaviors found during dev
  </action>
  <verify>dotnet build exits 0</verify>
  <done>AC-1, AC-2 satisfied</done>
</task>

<task type="auto">
  <name>Task 2: Data models + SQLite schema + repository</name>
  <files>LaCompta/Models/*.cs, LaCompta/Data/DatabaseContext.cs, LaCompta/Data/Repository.cs</files>
  <action>
    Models:
    - DailyRecord: Id, Season, Year, Day, FarmingIncome, ForagingIncome, FishingIncome, MiningIncome, OtherIncome, TotalExpenses
    - ItemTransaction: Id, DailyRecordId, ItemName, ItemId, Category, Quantity, UnitPrice, TotalPrice, CostBasis, Profit
    - SeasonSummary: Id, Season, Year, TotalIncome per category, BestDay, BestDayIncome, TotalExpenses
    - FishRecord: Id, FishName, FishId, IsLegendary, Quantity, TotalRevenue, Season, Year, Day

    DatabaseContext: SQLite init, CREATE TABLE IF NOT EXISTS, path = mod data folder
    Repository: AddDailyRecord, GetDailyRecords(season,year), AddItemTransaction, GetSeasonSummary, AddFishRecord, GetLegendaryFish

    Parallel agents: one for models, one for DB+repository
  </action>
  <verify>dotnet build succeeds with all new files</verify>
  <done>AC-3 satisfied</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Dev environment + mod skeleton + data layer</what-built>
  <how-to-verify>
    1. Launch Stardew Valley — SMAPI shows "LaCompta 0.1.0"
    2. No errors in console
    3. Confirm LaCompta.dll in mod folder
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<boundaries>
## DO NOT CHANGE
- Stardew Valley game files (beyond SMAPI)

## SCOPE LIMITS
- No event tracking logic yet (Plan 01-02)
- No web server (Phase 2)
- No Google APIs (Phase 3)
</boundaries>

<verification>
- [ ] `dotnet --version` returns 6.x
- [ ] SMAPI loads mod without errors
- [ ] `dotnet build` succeeds
- [ ] All models + repository compile
</verification>

<success_criteria>
- Dev environment functional
- Mod compiles and loads in SMAPI
- Data models + SQLite schema + repository ready
</success_criteria>

<output>
After completion, create `.paul/phases/01-scaffolding/01-01-SUMMARY.md`
</output>
